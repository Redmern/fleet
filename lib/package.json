{
  "name": "fleet-browser-test",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright driver for fleet agent browser-testing. Drives the system Chromium via playwright-core (no bundled-browser download).",
  "main": "browser-test.js",
  "dependencies": {
    "playwright-core": "^1.61.0"
  }
}
