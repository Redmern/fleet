__pycache__/
*.pyc

# vendored Playwright driver deps (one-time `cd lib && npm ci`; see install.sh).
# package-lock.json IS tracked + shipped (pins playwright-core for `npm ci`).
lib/node_modules/

# disposable browser-test screenshots dropped under each worktree
.fleet/shots/

# fleet runtime state (ready markers, dispatch ledger, pane-role registry)
.fleet/
